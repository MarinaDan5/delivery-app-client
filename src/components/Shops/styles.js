import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    header: {
        paddingBottom:'15px'
    },
    container: {
        borderRadius: 15,
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
    }
}));



