import { makeStyles, withThemeCreator } from "@mui/styles";
import { red } from '@mui/material/colors';

export const theme = {
    body: '#000',
    shadows: Array(25).fill('none')
}

// export const theme = withThemeCreator({
//     palette: {
//     primary: {
//       main: red[500],
//     },
//   }

// })